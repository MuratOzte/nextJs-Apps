'use client';

const Error = ({ error }) => {
    return (
        <main className="error">
            <h1>An Error occured!</h1>
            <p>Something went wrong</p>
        </main>
    );
};

export default Error;
